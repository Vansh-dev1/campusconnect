# CampusConnect

## Getting Started

- Clone the repository

    ```bash
    git clone https://github.com/Vansh-dev1/campusconnect.git
    cd campusconnect
    ```

- Install the dependency
    
    ```bash
    npm install
    ```

- Run the campusconnect
    
    ```bash
    npm start
    ```

